# Shopping Analytics Dashboard (Live)

Portfolio project:
- Python + SQL (PostgreSQL)
- Streamlit dashboard (interactive website)
- Deployable as a public live site from GitHub (Render)

## Local Run
1) Install deps:
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
